
<div>

  <div class='separator'>
    <h4><%= _("Merge Articles") %></h4>

    <% debugger %>

	<%= form_tag("/merge", method: "post") do %>
	  <%= label_tag(:merge_with, "Article ID: ") %>
	  <%= text_field_tag(:merge_with) %>
	  <%= hidden_field_tag(:parent_id, id) %>
	  <%= submit_tag("Merge") %>
	<% end %>
    
  </div>

</div>